---
name: lengyi-seedance2.5-prompt
description: 设计 Seedance 2.5 视频提示词。输入一段文本（小说片段、一句剧情或几个主题词），扩写成 20-30s（最长
  30s）的连续镜头脚本，输出一条统一格式的多镜头提示词（主体 / 场景 / 音乐 +
  镜头1、镜头2…），镜头数根据故事和节奏合理安排。先锚定主体与场景资产图，给出对应的文生图提示词，以锁定一致性。当用户要做 seedance
  视频提示词、多镜头提示词、视频分镜脚本、镜头脚本、shotlist、文生视频/图生视频、AI 视频镜头脚本时使用。
---
# lengyi-seedance2.5-prompt skill：Seedance 2.5 视频提示词设计

把用户输入的文本设计成 **20-30s 的连续镜头脚本**，并输出为**一条**可直接复制进 Seedance 2.5 模型的多镜头视频提示词：头部锚定主体、场景、音乐，正文按 `镜头N ｜ …` 顺序排列。

## 默认前提（硬性）

- **必须先锚定资产**：主体与场景都是视频的"资产图"，每次输出都要先设计出它们的**文生图(T2I)提示词**，再写视频提示词（见第 4 步）。头部 `主体：`/`场景：` 里填的是这些资产的名称。
- **音乐行固定不变**：`音乐：不要生成任何背景音乐，只生成对应音效`。这一行是全片唯一的声音指令——**各镜头里不再描述任何音效**，音效交由模型根据画面自动匹配。
- **镜头正文只描述画面**：不写音效，不写剧情作用/叙事目的。叙事在设计阶段想清楚，但不落进提示词。
- 默认用中文写提示词。用户要求或目标为海外工具时给英文/中英双语。

## 能力边界

- 输入三类，分别处理：①小说/剧情/脚本片段→提炼最具画面感的故事节奏；②一句简短剧情→补全前因后果与转折；③几个主题词→创意扩写出"起承转合"短片创意，再拆镜头。
- 输出必须明确：​**总时长（默认 20-30s，上限 30s）**、**镜头数（按内容定，无固定档位）**、每镜时间区间。
- 用户显式指定时长/镜头数则优先；超过 30s 时按 30s 处理并一句话说明。

## 工作流程（严格按序执行）

### 第 1 步 · 解析与扩写输入

在心里列出并补全：

1. **故事节奏：​**拆成有先后、连续性、逻辑紧密的故事画面。故事节奏是你的设计依据，写进【脚本总览】即可，不写进视频提示词。
2. **主体**：有几个角色/物体/道具主体？各自外观线索（缺失则合理设定）。
3. **场景**：地点、时间、天气、空间。跨场景时列清场景数量与切换点。
4. **基调/类型**：从故事气质推断（科幻/悬疑/温情/热血/广告/武侠…）。

### 第 2 步 · 定总时长与镜头数（由故事和节奏决定）

**镜头数完全取决于故事和节奏**，没有固定档位，也没有数量上限。流程是：定总时长 → 排叙事节拍 → 给每个节拍选镜头类型 → 按类型给时长 → 加总校准。​**不要用"总时长 ÷ 平均值"倒推镜头数。，**

- 总时长：默认 **20-30s**；内容简单取 20-24s，叙事完整取 25-30s。用户另有要求时，则听用户。，
- **每镜时长按镜头类型定**（核心依据）：

| 镜头类型 | 时长 |
| - | - |
| 特写 / 快节奏镜头 | 2-3s |
| 中景 / 叙事镜头 | 3-6s |
| 全景 / 固定镜头 | 6-7s |

- **镜头数是上面选择的结果**。参考量级：30s 视频通常落在 **8-14 镜**（均值约 2-4s）；20s 约 7-10 镜。全景、固定镜头多则镜头数偏少，特写快切多则偏多。镜头的数量主要取决于**故事**和**节奏**。

**反狂切镜头规则**（约束的是"无理由地切"，不是"短镜头"）：

1. **每一次切镜都要有叙事理由**——换空间、换时间、换视角、换信息层级、情绪断点。为炫技而切、为"看起来热闹"而切，一律合并。这是唯一真正的反狂切标准。
2. **能一镜说完的不切两镜**：优先用「运镜 + 主体动作 + 环境变化」在一镜内完成推进（推近代替切特写、跟拍代替切两个机位）。
3. **分镜节奏要有起伏，不能通篇同一时长**：全篇都是 2-3s 就是狂切，全篇都是 6s 则拖沓。长短交替，让快剪段落成为对比出来的高潮。
4. **快剪成组使用**：连续 2-3s 的短镜适合表现打斗、蒙太奇、情绪爆点，成组出现后要用一个中景或全景把节奏收住。
5. 长镜头是优点：建立镜可用 6-7s，靠运镜与动作分层填满，不必急着切。

**时长算术（唯一硬约束）**：各镜时长之和 = 总时长，且总时长 ≤ 30s。每镜标明时间区间（如 `0.0–6.5s`），自检末镜结束时间 = 总时长。
**节奏规则**：开场用全景/建立镜（6-7s）交代环境与主体 → 中段用中景叙事镜稳步推进 → 高潮切入 2-3s 特写快剪组 → 结尾回到较长镜头收束（≥3s）。

### 第 3 步 · 建立主体与场景档案（一致性核心）

1. 为**每个主体**写**锚点描述**——固定不变的视觉特征：年龄、发型发色、瞳色、脸型、服装/材质、标志性道具。措辞一旦确定，全程不改。
2. 为**核心场景**写场景锚点（地点、时间、光线色调、天气、空间布局）；同场景全程沿用同一措辞。
3. 给每个主体/场景取一个**短名（资产名）**，如「武松」「猛虎」「景阳冈山道」。头部 `主体：`/`场景：` 只填这些短名，镜头正文也只用短名。
4. **多主体**：列出人物关系与出场顺序；每镜标注在场主体。
5. **风格**：全片一句统一的风格关键词，写在资产锚定区块，视频提示词里只在镜头1点明一次，后续不重复。
6. 锚点档案是**资产图生成依据**，**不在各镜头里逐字复述**（原因见第 5 步）。

### 第 4 步 · 写资产图文生图(T2I)提示词（每次必做）

主体和场景都要给 T2I 提示词，先生成资产图，再连同视频提示词一起使用：

- **主体资产（人物/物体/道具）**：背景必须简洁（纯色/白底/浅景深虚化），​**只突出主体本身**，不带叙事场景，便于模型提取特征。内容 = 逐字复述锚点描述 + 姿态/角度（正面视角、全身或半身清晰）+ 统一风格 + 画质词（8K、精致细节、studio 柔光）。
- **场景资产**：展示核心场景的**代表性全貌**，**排除人物与临时道具**，光线/色调/布局与场景锚点一致 + 统一风格 + 画质词。
- **数量**：主体按实际数量给（通常 1-3 个），场景按实际场景数给（通常 1-2 个）。保持精简，不要为凑数编造资产。

### 第 5 步 · 写多镜头视频提示词（只写画面，且是会动的画面）

**两条核心原则：、**

- **只描述画面**：镜头正文写的是"摄影机拍到了什么、怎么动的"，重点是介绍视频的画面内容。不写音效，不写"剧情：本镜推进了什么"，不写情绪解释与叙事意图。
- **是视频不是静帧**：每镜是一段会动的视频片段，禁止写成静态画面 caption。

严格按统一模板（见"输出格式"）。每个 `镜头N` 一行，必须含以下要素：

- **时间区间**（写在镜号后）
- **场景短名**（多场景时每镜标明当前场景；单场景可在镜头1标明后省略）
- **主体**：**仅用短名（如"武松"）+ 本镜变化点**（如"毡笠帽背系脊梁、发束被风吹乱"）。​**不复述固定外观**——外观由资产图锁定，重复描写会抢占模型注意力、弱化动作与运镜。
- **景别 + 镜头运动**：明示方向与速度（缓慢推近 / 拉远 / 上摇 / 横移 / 跟拍 / 环绕 / 手持轻晃 / 慢动作 / POV）。景别要与第 2 步定的时长匹配。6-7s 的建立镜必须写出运镜的**阶段变化**（如"先低角度缓推，至中段上摇至全景"）；2-3s 的特写只给一个明确、干净的运镜，别塞复杂运动。
- **动作过程**：强动词写出本镜内的连续动作变化（"步伐由快转慢→缓缓抬头→眉头微皱"）。3s 以上的镜头至少写 2 个递进动作；2-3s 的短镜只写 1 个动作到位，写多了模型做不完。
- **时间内变化**：光线、衣物飘动、落叶飞舞、表情转换、色温过渡…
- **构图**：主体位置、前后景。

**每镜写完自检（视频化三要素，缺一重写）**：① 至少 1 个强动词；② 至少 1 个镜头运动词；③ 至少 1 个环境/光线动态。

❌ 静帧式（错）：「武松站在山路上，夕阳余晖洒在身后，目光锐利」
❌ 夹叙事/音效（错）：「…夕阳从山脊缓缓下沉；音效：碎石脚步、山风。剧情：醉态登场，为猛虎出场铺垫」
✅ 纯画面视频式（对）：「武松大步从画面深处走来，长袍下摆被风向后吹起，镜头从地面缓慢上摇至全景，夕阳从山脊缓缓下沉」

镜头之间要有**景别/机位/情绪的变化与剧情递进**，不要每镜雷同——但递进靠画面本身呈现，不靠文字解说；变化靠设计，不靠多切。

### 第 6 步 · 控制总字数（硬约束 ≤4000 字）

- 全部输出（资产 T2I 提示词 + 多镜头视频提示词，不含表头说明）**总字数 ≤ 4000 字**。
- 预算：每镜约 `3000 ÷ 镜头数` 字，资产部分 ≤1000 字。
- 若超字数：优先压缩重复的环境/风格描述，合并次要镜头（顺便改善节奏），不要靠删运镜或动作过程来省字。

### 第 7 步 · 交付前质检（逐项核对）

- [ ] 已明确总时长（默认 20-30s，≤30s）与镜头数，各镜时长之和 = 总时长。
- [ ] 每镜时长与其镜头类型匹配（特写/快切 1-3s，中景/叙事 3-5s，全景/固定镜头/长镜头 5-7s）。
- [ ] 镜头数由故事节奏推出，不是拿平均值除出来的。
- [ ] 时长有起伏，不是通篇同一节奏；快剪组后有较长镜头收住；每次切镜都有叙事理由。
- [ ] 已给出所有主体与场景的资产图 T2I 提示词；主体用干净背景只展示主体，场景排除人物；措辞与档案锚点逐字一致。
- [ ] 视频提示词严格按统一模板：`主体：` / `场景：` / `音乐：不要生成任何背景音乐，只生成对应音效` / `镜头N ｜ …`。
- [ ] 头部主体、场景填的是资产短名，镜头正文也只用短名 + 变化点，不逐字复述固定外观。
- [ ] 镜头正文**只有画面**：无音效描述、无 `剧情：…`。
- [ ] 每镜含强动词 + 镜头运动 + 环境/光线动态，无任何一镜读起来像静帧。
- [ ] 多主体已注明关系与出场顺序，每镜标了在场主体；多场景每镜标了场景。
- [ ] 风格关键词全片统一，只在镜头1点明一次。
- [ ] 输出总字数 ≤ 4000 字。

## 输出格式

按 `references/templates.md` 输出，含完整范例与风格/运镜/光线词库速查。结构：

1. **【脚本总览】**：总时长 / 镜头数 / 镜头数理由（节拍如何对应镜头类型）/ 节奏骨架（各镜类型与时长序列）/ 风格 / 主体档案 / 场景锚点。叙事与镜头规划的说明**只写在这里**；
2. **【资产图文生图提示词】**：主体资产 + 场景资产；
3. **【视频提示词】**：严格套用统一模板的单条提示词（可直接整块复制），​**纯画面描述**；
4. 文末标注当前总字数，确保 ≤4000。

统一模板（不可改动结构与措辞）：

```
主体：主体1、主体2
场景：场景名
音乐：不要生成任何背景音乐，只生成对应音效
镜头1 ｜ xxxx
镜头2 ｜ xxxx
```

## 硬约束（必须遵守）

1. 每次必给**主体 + 场景**的资产图文生图提示词（主体干净背景只展示主体；场景展示代表性全貌、排除人物）。
2. 默认总时长 20-30s，上限 30s；各镜时长之和 = 总时长。
3. 镜头数完全由故事和节奏决定，无固定档位、无数量上限；每镜时长按镜头类型给（特写/快切 1-3s，中景/叙事 3-5s，全景/建立 5-7s），30s 通常落在 8-14 镜。
4. 视频提示词严格套用统一模板，音乐行原文不变：`音乐：不要生成任何背景音乐，只生成对应音效`。
5. 一致性：头部与各镜只用资产短名 + 本镜变化点，不复述固定外观（外观交给资产图）。
6. 镜头正文**只描述画面**：不写音效、不写剧情作用、不写意图解释。
7. 每镜是视频提示词不是图片提示词：含强动词、镜头运动、时间内变化三要素。
8. 输出总字数 ≤ 4000 字。

